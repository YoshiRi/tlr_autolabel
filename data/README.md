Local generated dataset snapshots for manual inspection.

The dataset directories under this folder are intentionally git-ignored. They may
contain generated annotation tables and symlinks to large local WebAuto/T4 data.
